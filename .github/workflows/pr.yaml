name: PR workflow

on:
  pull_request:
    types: [opened, synchronize, reopened]


jobs:
  print-context:
    if: ${{ github.event.pull_request.head.ref == 'pr-branch-fake' }}
    uses: ./pr_call.yaml
